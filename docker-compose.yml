services:
  app:
    build: .
    ports:
      - "0.0.0.0:8000:8000"
    volumes:
      - db-data:/app/data
    env_file:
      - backend/.env
    environment:
      - DB_PATH=/app/data/dropship.db
      - CORS_ORIGINS=*
    restart: unless-stopped

volumes:
  db-data:
